@model ReserveWash.ViewModels.Product.CarWashServiceViewModel

@{
    ViewData["Title"] = "جزئیات سرویس";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var id = Url.ActionContext.RouteData.Values["id"];
}

<div class="container mt-4">
    <h1>جزئیات رزرو</h1>

    <div>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Name):
                @Html.DisplayFor(model => model.Name)
            </dt>
            <dd class="col-sm-10">
               @Html.DisplayNameFor(model => model.Price):
               @Html.DisplayFor(model => model.Price)
            </dd>
        </dl>
    </div>

    <div>
        <a class="btn btn-secondary" asp-action="Index" asp-route-id="">بازگشت به لیست</a>
    </div>
</div>
